export * from './create-theme';
export * from './types'; // Export our extended theme types
export * from './theme-config'; // Export themeConfig if it needs to be accessed directly
// Export core parts if direct access is ever needed, though usually create-theme is the entry point
export * from './core';
